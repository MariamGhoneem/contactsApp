# Appium server
appium.server.url=http://127.0.0.1:4723

# Device capabilities
deviceName=emulator-5554
platformVersion=14

# Built-in Contacts app (check with: adb shell dumpsys window | grep mCurrentFocus)
appPackage=com.android.contacts
appActivity=.activities.PeopleActivity

noReset=true
